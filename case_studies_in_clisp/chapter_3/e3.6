;;;Chapter3 exercise 3.6
(setf (symbol-value 'a) 'global-a)
(defvar *b* 'global-b)

(defun fn () *b*)

(defun test ()
  (let ((a 'local-a)
	(*b* 'local-b))
    (list a *b* (fn) (symbol-value 'a) (symbol-value '*b*))))